{
  "name": "bwr-project",
  "version": "1.0.0",
  "description": "BWR — La carte qui simplifie ta vie",
  "scripts": {
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create BWR_KV",
    "test": "node --test tests/graph-router.test.js"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
